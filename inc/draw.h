#include "sdl.struct.h"
#include "game.struct.h"

/* ---------------------------------------------------------------------------------------------------- */

void draw(struct SDL *sdl, struct Game *game);
